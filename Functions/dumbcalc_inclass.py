import math

def prod_quotient(a,b,c):
    result = (a + b) / c
    print('({} + {}) / {} = {:.2f}'.format(a, b, c, result))
    
def find_slope(x1,y1,x2,y2):
    rise = y2 - y1
    run = x2 - x1
    slope = rise / run
    print('The slope of a line through ({}, {}) and ({}, {}) is {}'.format(x1,y1,x2,y2,slope))

def distance(x1,y1,x2,y2):
    d = 
    
